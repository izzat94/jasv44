<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Announce extends Model
{
    protected $table = 'announces';

	// protected $fillable = ['subject','announcement', 'filename', 'mime', 'original_filename'];
}
